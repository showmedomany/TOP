<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
	<insert id="insertArticle" parameterType="board.BoardDataBean">
		insert into board(num, writer, email, subject
		, passwd, reg_date, ref, re_step, re_level, content) 
		values(board_seq.NEXTVAL, 
		#{writer}, #{email, jdbcType=VARCHAR}, #{subject}, #{passwd},
		#{reg_date}, #{ref}, #{re_step}, #{re_level}, #{content})
	</insert>


</mapper>