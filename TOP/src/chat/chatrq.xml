<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Rq">
	<select id="checkIp" parameterType="String" resultType="int">
		select count(*) from testchatrequest where ip = #{ip}
	</select>
	
	<insert id="insertRequest" parameterType="chat.RequestDataBean">
		insert into testchatrequest values(#{ip},#{id},sysdate,#{rp})
	</insert>
</mapper>